--- yaml
title: uns_20260305_164727_32447587
file_name: uns_20260305_164727_32447587.md
file_location: ~/anomalon_kernel\.intelligence\.narratives\.archive/
status: Goo
invariance_level: Tier 8
logic_system: Unclassified
substrate: Markdown
content_hash: c52ead6e0a0d48419f71aa9ee3a356c91278e667e529c4d2f92f87e0e3641976
version: 1
---
# Universal Narrative — 2026-03-05 16:47:27 UTC

**Run ID:** `32447587-c1ce-47de-ab82-45433c90b62f`  
**Timestamp:** 2026-03-05T16:47:27.481754+00:00  

---

## Act I: The Catalyst & Perception

### Original Prompt

```
# Lemma 4.2.1: Edge Functional Reduction

## Statement

Under Coboundary Locality (Axiom 1), the diagnostic system decomposes as:

$$J_P(s) = \sum_{e \in E_{\text{cov}}(P)} j_e(\delta_e(s))$$

where each $j_e : F(a) \to \mathbb{R}_{\geq 0}$ is a functional on the Banach stalk $F(a)$.

## Lean 4 Formalization Target

```lean
import Mathlib.Analysis.NormedSpace.Basic

/-- A finite poset with covering edges. -/
structure CoveringPoset where
  vertices : Type
  edges : List (vertices × vertices)

/-- A Banach presheaf assigns a normed space to each vertex. -/
structure BanachPresheaf (P : CoveringPoset) where
  stalk : P.vertices → Type
  [normed : ∀ v, NormedAddCommGroup (stalk v)]
  [normed_space : ∀ v, NormedSpace ℝ (stalk v)]

/-- A section assigns a vector in each stalk. -/
def Section (P : CoveringPoset) (F : BanachPresheaf P) :=
  ∀ v : P.vertices, F.stalk v

/-- The coboundary at an edge (a,b): δ_e(s) = r_{b,a}(s(b)) - s(a) -/
-- For simplicity, we model the coboundary as a map from sections to stalk vectors

/-- Axiom 1 (Coboundary Locality): J decomposes as a sum of edge functionals. -/
structure CoboundaryLocal (P : CoveringPoset) (F : BanachPresheaf P) where
  J : Section P F → ℝ
  j : P.vertices × P.vertices → F.stalk (Prod.fst ·) → ℝ  -- edge functional
  coboundary : ∀ e : P.vertices × P.vertices, Section P F → F.stalk e.1
  decomposition : ∀ s, J s = (P.edges.map (fun e => j e (coboundary e s))).sum

/-- Lemma 4.2.1: Under Coboundary Locality, J factors through edge functionals.
    This is essentially the content of Axiom 1 — it says the structure exists. -/
lemma edge_functional_reduction
    (P : CoveringPoset) (F : BanachPresheaf P)
    (ax : CoboundaryLocal P F) :
    ∃ (j : P.vertices × P.vertices → F.stalk (Prod.fst ·) → ℝ),
    ∀ s, ax.J s = (P.edges.map (fun e => j e (ax.coboundary e s))).sum :=
  ⟨ax.j, ax.decomposition⟩
```

## Purpose

This is the simplest lemma in Theorem 4.2's proof — it's essentially Axiom 1 restated
as a structural decomp
```

### Perceived Prompt

| Metric | Value |
|--------|-------|
| Class | `PromptClass.Definition` |
| Morphism | `MorphismType.Verify` |
| Entropy | 0.7240 |
| Coherence | 0.8314 |
| Decidability | 0.2760 |
| Hostility | 0.0000 |
| Status | LIQUID |

### QUIP

```xml
<SYSTEM>
morphism: Verify
output_type: bool
kernel: omega
constraint: logic_check
geometry: {entropy: 0.724, coherence: 0.831, decidability: 0.276, hostility: 0.000}
status: LIQUID
fingerprint: b0d29eacabffec95
</SYSTEM>
# Lemma 4.2.1: Edge Functional Reduction

## Statement

Under Coboundary Locality (Axiom 1), the diagnostic system decomposes as:

$$J_P(s) = \sum_{e \in E_{\text{cov}}(P)} j_e(\delta_e(s))$$

where each $j_e : F(a) \to \mathbb{R}_{\geq 0}$ is a functional on the Banach stalk $F(a)$.

## Lean 4 Formalization Target

```lean
import Mathlib.Analysis.NormedSpace.Basic

/-- A finite poset with covering edges. -/
structure CoveringPoset where
  vertices : Type
  edges : List (vertices × vertices)

/-- A Banach presheaf assigns a normed space to each vertex. -/
structure BanachPresheaf (P : CoveringPoset) where
  stalk : P.vertices → Type
  [normed : ∀ v, NormedAddCommGroup (stalk v)]
  [normed_space : ∀ v, NormedSpace ℝ (stalk v)]

/-- A section assigns a vector in each stalk. -/
def Section (P : CoveringPoset) (F : BanachPresheaf P) :=
  ∀ v : P.vertices, F.stalk v

/-- The coboundary at an edge (a,b): δ_e(s) = r_{b,a}(s(b)) - s(a) -/
-- For simplicity, we model the coboundary as a map from sections to stalk vectors

/-- Axiom 1 (Coboundary Locality): J decomposes as a sum of edge functionals. -/
structure CoboundaryLocal (P : CoveringPoset) (F : BanachPresheaf P) where
  J : Section P F → ℝ
  j : P.vertices × P.vertices → F.stalk (Prod.fst ·) → ℝ  -- edge functional
  coboundary : ∀ e : P.vertices × P.vertices, Section P F → F.stalk e.1
  decomposition : ∀ s, J s = (P.edges.map (fun e => j e (coboundary e s))).sum

/-- Lemma 4.2.1: Under Coboundary Locality, J factors through edge functionals.
    This is essentially the content of Axiom 1 — it says the structure exists. -/
lemma edge_functional_reduction
    (P : CoveringPoset) (F : BanachPresheaf P)
    (ax : CoboundaryLocal P F) :
    ∃ (j : P.vertices × P.vertices → F.stalk (Prod.fst ·) → ℝ),
    ∀ s, ax.J s = (P.edges.map (fun e => j e (ax.coboundary e s))).sum :=
  ⟨ax.j, ax.decomposition⟩
```

## Purpose

This is the simplest lemma in Theorem 4.2's proof — it's essentially Axiom 1 restated
as a structural decomposition. We use it to tune the Omega Engine pipeline:
- Cheap SLMs (gemma3:1b) generate compost
- codegemma:2b critiques
- Repeated failures → antibodies
- Gemini Flash validates

## Expected Outcome

Even cheap 1B models should be able to produce *something* for this lemma.
The failures will be predictable and repetitive — exactly what Bezdomny
needs to form real antibodies (≥3 identical failure modes).
```

### SBE Density Matrix

- **Purity:** 0.551088
- **Entropy:** 1.6177 bits
- **L1 Coherence:** 4.9691
- **Eigenvalues:** 28 of 28 non-zero

#### Top Off-Diagonal Coherences

| From → To | Re | Im | Magnitude |
|-----------|--------|--------|-----------|
| ⎵→∗ | +0.283814 | -0.014181 | 0.284168 |
| s→∗ | +0.093811 | +0.008286 | 0.094177 |
| e→∗ | +0.088083 | +0.020247 | 0.090380 |
| s→⎵ | +0.063757 | +0.006741 | 0.064113 |
| a→∗ | +0.053557 | -0.010930 | 0.054661 |
| e→⎵ | +0.051613 | +0.010967 | 0.052765 |
| p→∗ | +0.049356 | -0.003220 | 0.049461 |
| t→∗ | +0.044486 | -0.007593 | 0.045129 |
| f→∗ | +0.042971 | -0.010868 | 0.044324 |
| a→⎵ | +0.038371 | -0.003539 | 0.038534 |

### Immune System Antibodies

Loaded **1** constraints from compost history:

- RULE: complete proof required, never use sorry

### Proof Decomposition

**Decomposed into 5 lemmas:**

1. **covering_poset_def**: A `CoveringPoset` is a mathematical structure consisting of a type for its vertices and a list of ordered pairs representing its covering edges.
   ```lean
   structure CoveringPoset where
  vertices : Type
  edges : List (vertices × vertices)
   ```
2. **banach_presheaf_def**: A `BanachPresheaf` on a `CoveringPoset` `P` assigns to each vertex `v` in `P` a `NormedAddCommGroup` and `NormedSpace ℝ` (referred to as the stalk at `v`).
   ```lean
   structure BanachPresheaf (P : CoveringPoset) where
  stalk : P.vertices → Type
  [normed : ∀ v, NormedAddCommGroup (stalk v)]
  [normed_space : ∀ v, NormedSpace ℝ (stalk v)]
   ```
3. **section_def**: A `Section` of a `BanachPresheaf` `F` on a `CoveringPoset` `P` is a function that, for every vertex `v` in `P`, provides an element from the stalk `F.stalk v`.
   ```lean
   def Section (P : CoveringPoset) (F : BanachPresheaf P) :=
  ∀ v : P.vertices, F.stalk v
   ```
4. **coboundary_locality_axiom**: The `CoboundaryLocal` axiom defines a diagnostic system `J` and a `coboundary` operator such that `J` decomposes as a sum over the covering edges of `P`, where each term is an edge functional `j_e` applied to the `coboundary` value for that edge.
   ```lean
   structure CoboundaryLocal (P : CoveringPoset) (F : BanachPresheaf P) where
  J : Section P F → ℝ
  j : P.vertices × P.vertices → F.stalk (Prod.fst ·) → ℝ
  coboundary : ∀ e : P.vertices × P.vertices, Section P F → F.stalk e.1
  decomposition : ∀ s, J s = (P.edges.map (fun e => j e (coboundary e s))).sum
   ```
5. **edge_functional_reduction_theorem**: Given a `CoveringPoset` `P`, a `BanachPresheaf` `F`, and an instance of `CoboundaryLocal` for `P` and `F`, it is guaranteed that there exist edge functionals `j_e` such that the diagnostic system `J` decomposes as the sum of `j_e` applied to the `coboundary` values over all covering edges.
   ```lean
   lemma edge_functional_reduction
    (P : CoveringPoset) (F : BanachPresheaf P)
    (ax : CoboundaryLocal P F) :
    ∃ (j : P.vertices × P.vertices → F.stalk (Prod.fst ·) → ℝ),
    ∀ s, ax.J s = (P.edges.map (fun e => j e (ax.coboundary e s))).sum
   ```

### Mini-SBE Entropy Gate

✅ **PASSED**: Entropy 1.6177 bits ≤ threshold 1.8 bits
 — Purity 0.551088 — prompt coherent, proceeding to begemot generation.

---

## Act II: The Retinue Debate

### Begemot #1

**Model:** `gemini-2.5-flash` | **Temp:** 0.9


```lean
import Mathlib.Analysis.NormedSpace.Basic

/-- A finite poset with covering edges. -/
structure CoveringPoset where
  vertices : Type
  edges : List (vertices × vertices)

/-- A Banach presheaf assigns a normed space to each vertex. -/
structure BanachPresheaf (P : CoveringPoset) where
  stalk : P.vertices → Type
  [normed : ∀ v, NormedAddCommGroup (stalk v)]
  [normed_space : ∀ v, NormedSpace ℝ (stalk v)]

/-- A section assigns a vector in each stalk. -/
def Section (P : CoveringPoset) (F : BanachPresheaf P) :=
  ∀ v : P.vertices, F.stalk v

/-- Axiom 1 (Coboundary Locality): J decomposes as a sum of edge functionals. -/
structure CoboundaryLocal (P : CoveringPoset) (F : BanachPresheaf P) where
  J : Section P F → ℝ
  j : P.vertices × P.vertices → F.stalk (Prod.fst ·) → ℝ
  coboundary : ∀ e : P.vertices × P.vertices, Section P F → F.stalk e.1
  decomposition : ∀ s, J s = (P.edges.map (fun e => j e (coboundary e s))).sum

/-- Lemma 4.2.1: Under Coboundary Locality, J factors through edge functionals. -/
lemma edge_functional_reduction
    (P : CoveringPoset) (F : BanachPresheaf P)
    (ax : CoboundaryLocal P F) :
    ∃ (j : P.vertices × P.vertices → F.stalk (Prod.fst ·) → ℝ),
    ∀ s, ax.J s = (P.edges.map (fun e => j e (ax.coboundary e s))).sum :=
  ⟨ax.j, ax.decomposition⟩

```

### Begemot #2

**Model:** `gemini-2.5-flash` | **Temp:** 0.9


```lean
import Mathlib.Analysis.NormedSpace.Basic

/-- A finite poset with covering edges. -/
structure CoveringPoset where
  vertices : Type
  edges : List (vertices × vertices)

/-- A Banach presheaf assigns a normed space to each vertex. -/
structure BanachPresheaf (P : CoveringPoset) where
  stalk : P.vertices → Type
  [normed : ∀ v, NormedAddCommGroup (stalk v)]
  [normed_space : ∀ v, NormedSpace ℝ (stalk v)]

/-- A section assigns a vector in each stalk. -/
def Section (P : CoveringPoset) (F : BanachPresheaf P) :=
  ∀ v : P.vertices, F.stalk v

/-- Axiom 1 (Coboundary Locality): J decomposes as a sum of edge functionals. -/
structure CoboundaryLocal (P : CoveringPoset) (F : BanachPresheaf P) where
  J : Section P F → ℝ
  j : P.vertices × P.vertices → F.stalk (Prod.fst ·) → ℝ  -- edge functional
  coboundary : ∀ e : P.vertices × P.vertices, Section P F → F.stalk e.1
  decomposition : ∀ s, J s = (P.edges.map (fun e => j e (coboundary e s))).sum

/-- Lemma 4.2.1: Under Coboundary Locality, J factors through edge functionals.
    This is essentially the content of Axiom 1 — it says the structure exists. -/
lemma edge_functional_reduction
    (P : CoveringPoset) (F : BanachPresheaf P)
    (ax : CoboundaryLocal P F) :
    ∃ (j : P.vertices × P.vertices → F.stalk (Prod.fst ·) → ℝ),
    ∀ s, ax.J s = (P.edges.map (fun e => j e (ax.coboundary e s))).sum :=
  ⟨ax.j, ax.decomposition⟩

```

### Begemot #3

**Model:** `gemini-2.5-flash` | **Temp:** 0.9


```lean
import Mathlib.Analysis.NormedSpace.Basic

/-- A finite poset with covering edges. -/
structure CoveringPoset where
  vertices : Type
  edges : List (vertices × vertices)

/-- A Banach presheaf assigns a normed space to each vertex. -/
structure BanachPresheaf (P : CoveringPoset) where
  stalk : P.vertices → Type
  [normed : ∀ v, NormedAddCommGroup (stalk v)]
  [normed_space : ∀ v, NormedSpace ℝ (stalk v)]

/-- A section assigns a vector in each stalk. -/
def Section (P : CoveringPoset) (F : BanachPresheaf P) :=
  ∀ v : P.vertices, F.stalk v

/-- Axiom 1 (Coboundary Locality): J decomposes as a sum of edge functionals. -/
structure CoboundaryLocal (P : CoveringPoset) (F : BanachPresheaf P) where
  J : Section P F → ℝ
  j : P.vertices × P.vertices → F.stalk (Prod.fst ·) → ℝ  -- edge functional
  coboundary : ∀ e : P.vertices × P.vertices, Section P F → F.stalk e.1
  decomposition : ∀ s, J s = (P.edges.map (fun e => j e (coboundary e s))).sum

/-- Lemma 4.2.1: Under Coboundary Locality, J factors through edge functionals.
    This is essentially the content of Axiom 1 — it says the structure exists. -/
lemma edge_functional_reduction
    (P : CoveringPoset) (F : BanachPresheaf P)
    (ax : CoboundaryLocal P F) :
    ∃ (j : P.vertices × P.vertices → F.stalk (Prod.fst ·) → ℝ),
    ∀ s, ax.J s = (P.edges.map (fun e => j e (ax.coboundary e s))).sum :=
  ⟨ax.j, ax.decomposition⟩

```

### Koroviev's Critique

```
Rigor Rating: 3/10

The Lean formalization suffers from several critical flaws, primarily in the foundational definitions and a direct contradiction with the mathematical statement. While the final `lemma` itself is trivially correct *given its premises*, those premises are poorly constructed relative to the stated lemma.

Here are the precise flaws:

1.  **Mismatch in Codomain for `j_e` functional:**
    *   **Statement:** The lemma explicitly states "$j_e : F(a) \to \mathbb{R}_{\geq 0}$ is a functional on the Banach stalk $F(a)$." This means the functional must produce non-negative real numbers.
    *   **Lean Formalization:** The `CoboundaryLocal` structure defines `j : P.vertices × P.vertices → F.stalk (Prod.fst ·) → ℝ`. The codomain `ℝ` allows for *any* real number, including negative ones.
    *   **Impact:** This is a direct contradiction. The Lean definition permits behavior (negative values for $j_e$) that is explicitly forbidden by the mathematical statement of the lemma.

2.  **`CoveringPoset` is not a Poset, nor finite, nor "covering":**
    *   **Statement:** "A finite poset with covering edges."
    *   **Lean Formalization:** `structure CoveringPoset where vertices : Type; edges : List (vertices × vertices)`.
    *   **Impact:**
        *   `vertices : Type` does not imply finiteness. To be a "finite" poset, `P.vertices` should be constrained to be a `Fintype` (e.g., `(vertices : Type) [Fintype vertices]`).
        *   The structure merely defines a set of vertices and a list of pairs (edges). There are no axioms or properties enforcing a partial order relation (reflexivity, antisymmetry, transitivity) on `vertices`.
        *   There are no properties enforcing that the `edges` represent "covering edges" within a partial order. This definition is simply a directed graph, not a poset with covering relations. This is a major semantic gap.

3.  **`coboundary` is undefined and lacks presheaf structure:**
    *   **Statement:** The coboundary is explicitly defined as `$\delta_e(s) = r_{b,a}(s(b)) - s(a)$`. This definition crucially depends on the existence of restriction maps `r_{b,a}` inherent to a presheaf structure.
    *   **Lean Formalization:** The `CoboundaryLocal` structure includes `coboundary : ∀ e : P.vertices × P.vertices, Section P F → F.stalk e.1` but this is merely a type signature. It does not *define* what the `coboundary` function actually *is*.
    *   **Impact:** The `BanachPresheaf` structure is too minimal; it lacks the restriction maps (`r_{b,a}`) that are fundamental to defining the coboundary as stated. Without these maps and a concrete definition of `coboundary` in terms of them, the formalization cannot capture the essence of the mathematical statement's coboundary operation. It assumes `coboundary` exists with the correct type, but offers no way to verify it corresponds to `r_{b,a}(s(b)) - s(a)`.

4.  **Tautological Nature of the Lemma/Proof:**
    *   **Statement:** The system's preamble notes, "This is ess

... (4094 chars total)
```

---

## Act III: The Epistemic Sieve

### Woland Validation

```
Rigor Rating: 3/10

The Lean formalization suffers from several critical flaws, primarily in the foundational definitions and a direct contradiction with the mathematical statement. While the final `lemma` itself is trivially correct *given its premises*, those premises are poorly constructed relative to the stated lemma.

Here are the precise flaws:

1.  **Mismatch in Codomain for `j_e` functional:**
    *   **Statement:** The lemma explicitly states "$j_e : F(a) \to \mathbb{R}_{\geq 0}$ is a functional on the Banach stalk $F(a)$." This means the functional must produce non-negative real numbers.
    *   **Lean Formalization:** The `CoboundaryLocal` structure defines `j : P.vertices × P.vertices → F.stalk (Prod.fst ·) → ℝ`. The codomain `ℝ` allows for *any* real number, including negative ones.
    *   **Impact:** This is a direct contradiction. The Lean definition permits behavior (negative values for $j_e$) that is explicitly forbidden by the mathematical statement of the lemma. This is a critical error.

2.  **`CoveringPoset` is not a Poset, nor finite, nor "covering":**
    *   **Statement:** "A finite poset with covering edges."
    *   **Lean Formalization:** `structure CoveringPoset where vertices : Type; edges : List (vertices × vertices)`.
    *   **Impact:**
        *   `vertices : Type` does not imply finiteness. To be a "finite" poset, `P.vertices` should be constrained to be a `Fintype` (e.g., `(vertices : Type) [Fintype vertices]`).
        *   The structure merely defines a set of vertices and a list of pairs (edges). There are no axioms or properties enforcing a partial order relation (reflexivity, antisymmetry, transitivity) on `vertices`.
        *   There are no properties enforcing that the `edges` represent "covering edges" within a partial order (i.e., if `(a,b)` is an edge, there is no `c` such that `a < c < b`). This definition is simply a directed graph, not a poset with covering relations. This is a major semantic gap between the mathematical statement and its formalization.

3.  **`coboundary` is abstract and lacks specific structure:**
    *   **Statement:** The coboundary is explicitly defined as `δ_e(s) = r_{b,a}(s(b)) - s(a)`.
    *   **Lean Formalization:** The `CoboundaryLocal` structure defines `coboundary : ∀ e : P.vertices × P.vertices, Section P F → F.stalk e.1`.
    *   **Impact:** While the problem statement notes "For simplicity, we model the coboundary as a map from sections to stalk vectors," this abstraction completely omits the specific definition involving restriction maps and subtraction. This means the formalization does not capture the *meaning* or *structure* of a coboundary operator, only its input/output types. For *this specific lemma*, it doesn't cause a direct proof failure because the lemma only asserts the existence of such a function, but it severely limits the utility for subsequent lemmas that might rely on the internal structure of `δ_e`.

4.  **Lemma's Triviality:**
    *   **Statement
```

---

## Act V: The Final Verdict

### Master's Verdict

```json
```json
{
  "verdict": "COMPOSTED",
  "epistemic_value": 0.1,
  "quality": 0.1,
  "reason": "The Lean 4 formalization fundamentally misrepresents the mathematical statement of Lemma 4.2.1 and its underlying Axiom 1 in several critical ways. The most severe flaw is a direct contradiction in the type signature of the edge functional `j_e`. The statement explicitly requires `j_e : F(a) \to \\mathbb{R}_{\\geq 0}`, meaning it must produce non-negative real numbers. However, the `CoboundaryLocal` structure defines `j` with a codomain of `\\mathbb{R}`, which permits negative values. This is a fatal logical gap, as the formalized axiom allows behavior explicitly forbidden by the mathematical statement.\n\nFurthermore, the `CoveringPoset` structure is severely under-specified. It defines `vertices : Type` without enforcing finiteness (e.g., `[Fintype vertices]`) and `edges : List (vertices \\times vertices)` without any axioms to establish a partial order relation or the 'covering edge' property. It merely describes a generic directed graph, not a finite poset with covering edges as stated. This is a significant semantic mismatch.\n\nFinally, while the lemma's proof itself is trivially correct given the flawed definitions (it merely extracts fields from the `CoboundaryLocal` structure), the definitions upon which it relies are unfaithful to the problem's mathematical description. The `coboundary` function is also overly abstract, lacking the specific presheaf structure implied by the mathematical context (e.g., involving restriction maps `r_{b,a}`).",
  "failure_modes": [
    "Type Mismatch (Codomain of j_e)",
    "Semantic Mismatch (CoveringPoset Definition)",
    "Under-specification (Coboundary Operator)",
    "Axiom Misrepresentation"
  ]
}
```
```

### Trace Summary

| Field | Value |
|-------|-------|
| Run ID | `32447587-c1ce-47de-ab82-45433c90b62f` |
| Verdict | **COMPOSTED** |
| Model | `gemini-2.5-flash` |
| Class | `PromptClass.Definition` |
| Begemots | 3 generated |

*Generated by `run_omega.py solve` at 2026-03-05T16:47:27.481754+00:00*

---

## Act VI: The Intern's Report

### Data Pipeline Audit

| File | Records | Status |
|------|---------|--------|
| `unified_oe_trace.jsonl` | 16 | ✅ |
| `master_journal.jsonl` | 34 | ✅ |
| `prompt_fitness.jsonl` | 22 | ✅ |
| `prompt_genealogy.jsonl` | 5 | ✅ |
| Compost entries | 36 | ✅ |
| Antibodies | 0 | ⏳ Farming |

### Failure Pattern Accumulation

| Failure Mode | Count | Antibody Status |
|-------------|-------|-----------------|
| `incomplete_proof` | 3 | 🛡️ **ANTIBODY READY** |
| `false_lemma` | 2 | ⚠️ 1 more → antibody |
| `tautological_lemma` | 1 | 📊 Tracking |
| `underspecified_mathematical_structure` | 1 | 📊 Tracking |
| `missing_constructive_definition` | 1 | 📊 Tracking |
| `misinterpretation_of_lemma_role` | 1 | 📊 Tracking |
| `missing_completeness_property` | 1 | 📊 Tracking |
| `missing_proof` | 1 | 📊 Tracking |
| `dummy_definitions` | 1 | 📊 Tracking |
| `fragmented_submission` | 1 | 📊 Tracking |
| `typeclass_resolution_error` | 1 | 📊 Tracking |
| `syntax_error` | 1 | 📊 Tracking |
| `overcomplication` | 1 | 📊 Tracking |
| `ignored_hypothesis` | 1 | 📊 Tracking |
| `parse_error` | 1 | 📊 Tracking |

### Proposed Rules (from recurring failures)

> **Pattern `incomplete_proof` (3×):** Proofs must be complete — no sorry, no gaps, no unfinished cases.

### This Run's Failures (4)

- `Type Mismatch (Codomain of j_e)` — now at **1×** across all runs
- `Semantic Mismatch (CoveringPoset Definition)` — now at **1×** across all runs
- `Under-specification (Coboundary Operator)` — now at **1×** across all runs
- `Axiom Misrepresentation` — now at **1×** across all runs

